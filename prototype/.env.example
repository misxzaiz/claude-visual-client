# Claude Code Git Bash 路径 (Windows 需要)
CLAUDE_CODE_GIT_BASH_PATH=D:\Program Files\Git\bin\bash.exe

# 服务器端口
PORT=3000

# 工作目录
WORK_DIR=D:\claude

# Claude 权限模式
# default              - 每次操作需要确认
# dontAsk              - 不询问但自动拒绝危险操作（写入/Bash被禁用）
# bypassPermissions    - 绕过权限检查，自动允许所有操作 ⚠️ (原型推荐)
# acceptEdits          - 自动允许编辑操作
# delegate             - 委托模式
PERMISSION_MODE=bypassPermissions

# Claude 命令路径 (可选，默认使用系统路径)
# CLAUDE_CMD=claude
