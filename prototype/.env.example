# Claude Code Git Bash 路径 (Windows 需要)
CLAUDE_CODE_GIT_BASH_PATH=D:\Program Files\Git\bin\bash.exe

# 服务器端口
PORT=3000

# 工作目录
WORK_DIR=D:\claude

# Claude 权限模式
# default    - 需要确认每个操作 (交互式，Web 界面下可能不可用)
# dontAsk    - 自动允许所有操作 (原型开发推荐)
# read-only  - 只读模式，不允许写入
PERMISSION_MODE=dontAsk

# Claude 命令路径 (可选，默认使用系统路径)
# CLAUDE_CMD=claude
